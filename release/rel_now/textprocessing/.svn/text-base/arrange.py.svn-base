import os, sys

ith = int(sys.argv[2])
curr = ''
for line in open(sys.argv[1]):
	#print line,
	es = line.split()
	if not curr==es[ith]:
		if not curr=='':
			print
		curr = es[ith]
	print es[1],		
